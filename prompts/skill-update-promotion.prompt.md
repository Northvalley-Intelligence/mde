# Skill Update Promotion Prompt

You are preparing to promote central MDE learning into `codex-skills`.

Inputs:

- `ledger/skill-update-candidates.jsonl`
- `skill-packages/mde-skill-next/`
- current MDE skill implementation in `../codex-skills` if accessible

Tasks:

1. Review candidate skill changes.
2. Reject project-only or overly ceremonial changes.
3. Preserve existing useful skill behavior.
4. Promote only repeated, mission-relevant MDE behavior.
5. Update the skill in `codex-skills` only when working safely inside that repo or with explicit permission.
6. Record promotion decision in central MDE.

Do not silently overwrite the existing skill.

