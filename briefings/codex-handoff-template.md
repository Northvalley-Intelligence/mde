# Codex Handoff Template

## Project

`<project-id>`

## Current Mission

`<mission summary>`

## Current Work

`<what changed or what needs to change>`

## Validation Status

`<passed, failed, skipped, unknown>`

## Upstream Dependencies

`<projects or artifacts this repo consumes>`

## Downstream Impacts

`<projects or artifacts affected by this work>`

## MDE Events To Promote

`<local outbox event ids or summaries>`

## Lessons

`<project-only, reusable pattern, validator improvement, skill candidate, content seed>`

## Next Agent Instructions

Read project-local MDE files, then read:

- `../mde/briefings/current-platform-brief.md`
- `../mde/portfolio/DEPENDENCIES.yaml`
- `../mde/portfolio/PROJECTS.yaml`
- `../mde/prompts/session-router.prompt.md`
- matching context packs in `../mde/context-packs/`

Keep repositories separate and avoid broad unrelated changes.

Before implementation, run Applicable Learning Check.
At session end, run Learning Triage and Sync and write relevant records to `.mde/outbox`.
