# Skill Packages

Skill packages are proposed updates for `codex-skills`.

They let central MDE prepare skill behavior without silently overwriting the actual skill implementation.

Current package:

- `mde-skill-next`

