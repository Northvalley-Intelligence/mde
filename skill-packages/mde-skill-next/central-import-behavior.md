# Central Import Behavior

Central MDE import should:

- read project outbox files
- avoid duplicate records
- preserve confidentiality labels
- import reusable lessons and cross-project impacts
- update briefings when platform context changes
- leave project-only noise in the project repo
- treat project repos as read-only during central imports
- report outbox-bearing sibling repos that are outside the central portfolio
